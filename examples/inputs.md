# Example Inputs

```bash
npm run dev -- --mode banalizer "We are thrilled to embark on a transformational journey to unlock data-driven value across the enterprise."
```

```bash
npm run dev -- --mode romcom "supplier deduplication"
```

```bash
npm run dev -- --mode archaeology "why invoices map to the wrong supplier"
```

```bash
npm run dev -- --mode architecture "global ERP template"
```
