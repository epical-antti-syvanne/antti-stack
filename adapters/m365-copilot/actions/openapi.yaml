openapi: 3.1.0
info:
  title: Antti Stack Actions
  version: 0.1.0
  description: Placeholder OpenAPI contract for future Microsoft 365 Copilot actions.
servers:
  - url: https://example.invalid/api
paths:
  /diagnose:
    post:
      operationId: diagnose
      summary: Diagnose workplace text
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [input]
              properties:
                input:
                  type: string
                intensity:
                  type: string
                  enum: [safe, default, more-edge]
                  default: safe
      responses:
        "200":
          description: AgentResponse JSON
          content:
            application/json:
              schema:
                type: object
  /codec:
    post:
      operationId: codec
      summary: Run Satire Codec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [direction, input]
              properties:
                direction:
                  type: string
                  enum: [reduce, induce]
                input:
                  type: string
      responses:
        "200":
          description: TransformTextResult JSON
          content:
            application/json:
              schema:
                type: object